export default function alarma(btnStartAlarm,btnStopAlarm){
    const d = document;

    d.addEventListener("click",(e) => {

        if (e.target.matches(btnStartAlarm)){
           // window.matchMedia.
        }
        if (e.target.matches(btnStopAlarm)){
            //
        }

    });
}